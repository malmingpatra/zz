[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  CI = "false"
  NPM_FLAGS = "--legacy-peer-deps"

[install]
  command = "npm install --legacy-peer-deps"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
